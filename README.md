# Valentine Streamlit App

Run the app with:

```bash
pip install -r requirements.txt
streamlit run app.py
```

- Click the red "No" link to make it shrink. After 5 presses it will be hidden behind the green "Yes" link.
- Click the green "Yes" link to accept.
